/*
 * kalmanFilterC.c
 *
 *  Created on: Jan 23, 2021
 *      Author: ah
 */
#include "main.h"
#include "lab1util.h"

int kalmanFilterL(float* InputArray, float* OutputArray, struct KalmanState* kstate, int length){
	float sum = 0;
//	for(int i = 0; i < length; i++){
//		sum = sum + array[i];
//	}
//	float mean = sum / (float) size;
//	float varsum = 0;
//	for(uint i = 0; i < size; i++){
//		varsum = varsum + powf(array[i] - mean,2);
//	}
//
//	for (int i = 0; n < length; n++){
//		for (int j = 0; k < length; k++){
//			r[n] += (k < n1 ? v1[k] : 0) * (n - k < n2 ? v2[n - k] : 0);
//		}
//	}

	return 0;
}
